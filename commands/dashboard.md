---
description: Start (or stop) the local Hone dashboard — skill profile and counters at http://127.0.0.1:4173
argument-hint: "[stop]"
disable-model-invocation: true
---

!`node "${CLAUDE_PLUGIN_ROOT}/bin/hone-ctl.ts" dashboard $ARGUMENTS`

Relay the output above in one sentence (include the URL if the dashboard
started). Do not open a browser yourself.
