{
  "name": "hone-claude-plugin",
  "version": "0.6.0",
  "private": true,
  "description": "Hone — a Claude Code and OpenCode plugin that makes developers better engineers, not just faster ones. Intent routing, learning budget, solution gate, hint levels, Socratic reviews.",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "bin": {
    "hone-ctl": "bin/hone-ctl.ts"
  },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  },
  "license": "MIT"
}
